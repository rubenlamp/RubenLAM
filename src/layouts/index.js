import contact from './contact';
import home from './home';
import page from './page';
import post from './post';
import gallery from './gallery';
console.log('Pasé por layouts/index.js');
export { contact, home, page, post, gallery };

export default {
    contact,
    home,
    page,
    post,
    gallery
};
